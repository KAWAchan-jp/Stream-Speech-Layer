# AGENTS.md

このリポジトリでは、Stream Speech Layer の Chrome / Brave 向け Chromium 拡張としての目的を優先して作業する。

## 基本方針

- 会話、説明、作業メモは日本語を基本にする。
- 主要対象は YouTube / Twitch などの配信音声であり、チャット本文ではなくタブ音声を主対象にする。
- MVP は「音声取得 -> チャンク化 -> 音声認識 -> 字幕表示 -> ログ保存」の最小フローを優先する。
- まずは単一サイト・単一経路で確実に動く形を作り、その後で対応サイトやエンジンを広げる。
- 変更は小さく、可逆性とレビューしやすさを保つ。

## 重要な参照先

- `README.md`: 機能要件、使い方、ユーザー向け説明の中心。
- `README.en.md`: 英語版 README。ユーザー向け仕様を変えた場合は必要に応じて同期する。
- `docs/`: 設計メモ、プライバシー方針、認識エンジンごとの補足。
- `extension/manifest.json`: Manifest V3 の権限、host permissions、拡張の構成。
- `extension/src/`: background、offscreen、content script、UI、認識処理の実装。

## 実装上の優先順位

1. ユーザー操作による開始・停止を明確にする。
2. タブ音声取得と再出力を壊さない。
3. MediaRecorder による音声チャンク化を安定させる。
4. 認識エンジンと翻訳エンジンは差し替え可能な境界を保つ。
5. 字幕オーバーレイ、popup、options の状態表示を分かりやすく保つ。
6. ログ保存は `chrome.storage.local` を基本にし、保存件数や保存内容を README と一致させる。

## プライバシーと権限

- 録音、タブ音声取得、外部 API 送信は、明示的なユーザー操作後に限定する。
- API キーをコードやドキュメント例へ直書きしない。
- 新しい `permissions` や `host_permissions` を追加する場合は、理由と影響範囲を説明する。
- 外部送信先を追加・変更した場合は、README や `docs/privacy-notes.md` も確認する。
- 取得・保存するデータの範囲を広げる場合は、削除方法や保存上限も合わせて考える。

## Chrome 拡張としての注意

- Manifest V3 と `chrome.*` API を前提にする。
- Chrome と Brave の両方を意識する。Firefox 固有の実装は基本的に入れない。
- service worker、offscreen document、content script の責務を混ぜすぎない。
- background は権限取得と全体制御、offscreen は音声処理、content script はページ上 UI と表示制御を中心に扱う。
- popup / options はユーザーが現在状態、設定、API キー保存状態を理解できる UI にする。

## ドキュメント同期

- 実装済み機能、設定項目、保存データ、外部送信先が変わった場合は README を更新する。
- 設計判断や今後の方針が変わった場合は `docs/` の該当メモを更新する。
- README と実装が矛盾しないようにする。
- ユーザー向けの説明では、無料枠や外部サービスの制約を断定しすぎず、確認先も残す。

## 避けること

- 仕様が曖昧なまま大きな再設計を始める。
- API キー、個人情報、配信ログの実データをコミットする。
- ユーザー同意なしに音声取得や外部送信が始まる挙動を作る。
- README や docs と矛盾する UI 文言・保存仕様・権限を追加する。
- 既存の未コミット変更を理由なく整形・巻き戻しする。

## 検証観点

- 拡張を読み込めるか。
- popup から開始・停止できるか。
- YouTube / Twitch の対象タブで字幕オーバーレイが出るか。
- タブ音声が無音化せず再出力されるか。
- 認識エンジン未設定時に外部送信せず、音声取得までで止まるか。
- API 上限やエラー時にユーザーへ分かる形で表示されるか。
- options の設定保存、API キー保存状態、字幕スタイル保存が期待通りか。

## 作業後の報告

- 何を変えたか、どのファイルを触ったかを簡潔に報告する。
- 実行した検証と、未実施の検証があれば明記する。
- 権限、外部 API、保存データに関わる変更では、プライバシー上の影響も一言添える。
